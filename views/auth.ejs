<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Cuenta - GamerVision</title>
    
    <!-- Tailwind CSS -->
    <script src="https://cdn.tailwindcss.com"></script>
    <!-- Iconos -->
    <script src="https://unpkg.com/lucide@latest"></script>
    <!-- Fuentes -->
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;700;900&display=swap" rel="stylesheet">

    <style>
        body { font-family: 'Roboto', sans-serif; background-color: #121212; color: #e0e0e0; }
        
        /* Inputs Generales */
        .custom-input { background-color: #0f0f0f; border: 1px solid #333; color: white; transition: all 0.3s; }
        .custom-input:focus { border-color: #ff6b00; outline: none; box-shadow: 0 0 0 1px rgba(255, 107, 0, 0.5); }
    </style>
</head>
<body class="flex flex-col min-h-screen">

    <!-- HEADER SIMPLIFICADO (Para navegación básica) -->
    <header class="bg-[#1a1a1a] sticky top-0 z-50 shadow-lg border-b border-gray-800">
        <div class="container mx-auto px-4 py-4 flex justify-between items-center">
            <a href="/" class="flex items-center gap-2 flex-shrink-0 cursor-pointer">
                <div class="w-10 h-10 bg-white rounded-full flex items-center justify-center text-black font-bold text-xl">G</div>
                <div class="flex flex-col">
                    <span class="text-2xl font-black tracking-tighter text-white">GAMER<span class="text-[#ff6b00]">VISIÓN</span></span>
                </div>
            </a>
            <a href="/" class="text-sm text-gray-400 hover:text-white flex items-center gap-2">
                <i data-lucide="arrow-left" class="w-4 h-4"></i> Volver a la Tienda
            </a>
        </div>
    </header>

    <!-- *************************************************************** -->
    <!--                SECCIÓN 3: MI CUENTA (LOGIN/REGISTER)            -->
    <!-- *************************************************************** -->
    <div class="flex-1 py-16 bg-[#121212] flex items-center">
        <div class="container mx-auto px-4">
            <h2 class="text-3xl font-black text-white mb-10 text-center uppercase">Mi Cuenta</h2>
            
            <!-- ALERTAS BACKEND -->
            <% if (typeof error !== 'undefined') { %>
                <div class="max-w-md mx-auto mb-6 bg-red-900/50 border border-red-500 text-red-200 px-4 py-3 rounded text-center"><%= error %></div>
            <% } %>
            <% if (typeof success !== 'undefined') { %>
                <div class="max-w-md mx-auto mb-6 bg-green-900/50 border border-green-500 text-green-200 px-4 py-3 rounded text-center"><%= success %></div>
            <% } %>

            <div class="grid grid-cols-1 md:grid-cols-2 gap-12 max-w-5xl mx-auto">
                
                <!-- ACCEDER -->
                <div class="bg-[#1a1a1a] p-8 rounded-lg border border-gray-800 shadow-lg">
                    <h3 class="text-xl font-bold text-white mb-6 uppercase border-b border-gray-700 pb-4">Acceder</h3>
                    <form action="/auth/login" method="POST">
                        <div class="mb-5">
                            <label class="block text-gray-400 text-sm font-bold mb-2">Correo electrónico <span class="text-[#ff6b00]">*</span></label>
                            <input class="custom-input w-full p-3 rounded text-sm" type="email" name="email" required>
                        </div>
                        <div class="mb-6 relative">
                            <label class="block text-gray-400 text-sm font-bold mb-2">Contraseña <span class="text-[#ff6b00]">*</span></label>
                            <input id="login-pass" class="custom-input w-full p-3 rounded text-sm pr-10" type="password" name="password" required>
                            <button type="button" onclick="togglePassword('login-pass')" class="absolute right-3 top-9 text-gray-500 hover:text-white">
                                <i data-lucide="eye" class="w-5 h-5"></i>
                            </button>
                        </div>
                        <button class="w-full bg-[#ff6b00] hover:bg-[#e65100] text-white font-bold py-3 rounded transition">ACCEDER</button>
                    </form>
                    <div class="mt-4 text-center">
                        <a href="#" onclick="alert('Contacta al admin para restablecer')" class="text-xs text-gray-500 hover:text-[#ff6b00]">¿Olvidaste tu contraseña?</a>
                    </div>
                </div>

                <!-- REGISTRARSE -->
                <div class="bg-[#1a1a1a] p-8 rounded-lg border border-gray-800 shadow-lg">
                    <h3 class="text-xl font-bold text-white mb-6 uppercase border-b border-gray-700 pb-4">Registrarse</h3>
                    <form action="/auth/register" method="POST">
                        <div class="mb-5">
                            <label class="block text-gray-400 text-sm font-bold mb-2">Nombre de usuario <span class="text-[#ff6b00]">*</span></label>
                            <input class="custom-input w-full p-3 rounded text-sm" type="text" name="nombre" required>
                        </div>
                        <div class="mb-5">
                            <label class="block text-gray-400 text-sm font-bold mb-2">Correo electrónico <span class="text-[#ff6b00]">*</span></label>
                            <input class="custom-input w-full p-3 rounded text-sm" type="email" name="email" required>
                        </div>
                        <div class="mb-6 relative">
                            <label class="block text-gray-400 text-sm font-bold mb-2">Contraseña <span class="text-[#ff6b00]">*</span></label>
                            <input id="reg-pass" class="custom-input w-full p-3 rounded text-sm pr-10" type="password" name="password" required>
                            <button type="button" onclick="togglePassword('reg-pass')" class="absolute right-3 top-9 text-gray-500 hover:text-white">
                                <i data-lucide="eye" class="w-5 h-5"></i>
                            </button>
                        </div>
                        <button class="w-full border border-white hover:bg-white hover:text-black text-white font-bold py-3 rounded transition">REGISTRARSE</button>
                    </form>
                </div>

            </div>
        </div>
    </div>
    
    <!-- LÓGICA JAVASCRIPT -->
    <script>
        lucide.createIcons();

        // --- TOGGLE PASSWORD (Lógica del Ojito) ---
        function togglePassword(inputId) {
            const input = document.getElementById(inputId);
            if (input.type === "password") {
                input.type = "text";
            } else {
                input.type = "password";
            }
        }
    </script>
</body>
</html>